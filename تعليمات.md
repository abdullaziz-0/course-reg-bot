# تعليمات تشغيل البوت

## 1. صيغة الرسالة الثابتة اللي يبعثها الطالب

```
الاسم: محمد ابراهيم
الجوال: 0501234567
تلقرام: @mohammed_ib
المادة: برمجة1
السعر: 250
المدفوع: 100
```

⚠️ مهم: اسم المادة في الرسالة لازم يطابق **بالضبط** اسم الشيت (Tab) في Google Sheet
(نفس الحروف والمسافات — "برمجة1" غير "برمجة 1").

## 2. رفع الكود على GitHub (أسهل طريقة لربطه بـ Railway)

1. سوّي حساب GitHub لو ما عندك
2. سوّي Repository جديد (خاص/Private)
3. ارفع فيه هذي الملفات: bot.py, requirements.txt, Procfile

## 3. النشر على Railway

1. روح لـ railway.app وسجل دخول (تقدر تسجل بحساب GitHub مباشرة)
2. New Project > Deploy from GitHub repo > اختر الـ repo اللي رفعته
3. بعد ما ينشئ المشروع، روح لتبويب **Variables** وضيف:
   - `BOT_TOKEN` = التوكن اللي أعطاك ياه BotFather
   - `SHEET_NAME` = طلاب 481
   - `GOOGLE_CREDS_JSON` = افتح ملف الـ JSON اللي نزّلته، انسخ **محتواه بالكامل** (كله سطر واحد أو عدة أسطر، ما يفرق) والصقه كقيمة كاملة لهذا المتغير
4. Railway بيقرأ Procfile تلقائيًا ويشغّل `python bot.py`
5. من تبويب **Deployments** تقدر تشوف اللوق (Logs) وتتأكد إنه طبع "Bot started, polling..."

## 4. التجربة

روح لبوتك @courseRegbot وابعث له رسالة بالصيغة أعلاه، وشوف يرد عليك ويضيف السطر في شيت "طلاب 481" بالشيت الصحيح حسب اسم المادة.

## ملاحظات

- لو المادة غير موجودة كشيت، البوت يرد للطالب بخطأ واضح بدل ما يفشل بصمت.
- لو الرسالة ناقصة سطر، البوت يوضح للطالب الصيغة المطلوبة كاملة.
- التاريخ (تاريخ آخر دفعة) يتسجل تلقائيًا بتاريخ اليوم.
